<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wini.restapitemplate.user.mapper.UserMapper">

    <select id="getUser" resultType="com.wini.restapitemplate.user.model.User">
        SELECT id, name
        FROM users
        WHERE id = #{id}
    </select>

    <insert id="insertUser">
        INSERT INTO users(id, name, password, enabled)
        VALUES (#{id}, #{name}, #{password}, true)
    </insert>
</mapper>